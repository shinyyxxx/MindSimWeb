server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # SPA fallback so deep links (e.g. /simulation) refresh correctly.
  location / {
    try_files $uri $uri/ /index.html;
  }
}
