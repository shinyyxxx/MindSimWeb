# newClient (Vite + React)

Frontend-only React demo for Mindsim (no backend).

## Run

```powershell
cd newClient
npm install
npm run dev
```

Then open the URL Vite prints (usually `http://localhost:5173`).

## Assets

Assets live in `newClient/public/assets` and are served at `/assets/...`.
